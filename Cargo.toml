[workspace]
members = [
    "services/portfolio-api",
    "tools/seed",
    "shared/emulsion-types",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
